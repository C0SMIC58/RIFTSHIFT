const $=(q,s=document)=>s.querySelector(q), $$=(q,s=document)=>[...s.querySelectorAll(q)];
const rootDepth=location.pathname.split('/').filter(Boolean).length>1?'../':'';
window.addEventListener('scroll',()=>$('.site-header')?.classList.toggle('scrolled',scrollY>12));
$('.menu-btn')?.addEventListener('click',()=>$('.nav')?.classList.toggle('open'));
const here=location.pathname.replace(/index\.html$/,'');
$$('.navlinks a').forEach(a=>{const u=new URL(a.href,location.href).pathname.replace(/index\.html$/,''); if((here.endsWith('/RIFTSHIFT/')&&u.endsWith('/RIFTSHIFT/'))||(!here.endsWith('/RIFTSHIFT/')&&here.startsWith(u)))a.classList.add('active')});
const io=new IntersectionObserver(es=>es.forEach(e=>e.isIntersecting&&e.target.classList.add('in')),{threshold:.12}); $$('.reveal').forEach(el=>io.observe(el));
$$('[data-accordion]').forEach(btn=>btn.addEventListener('click',()=>{const body=btn.nextElementSibling, open=body.style.maxHeight; body.style.maxHeight=open?'':body.scrollHeight+'px'; btn.setAttribute('aria-expanded',open?'false':'true')}));
const launch=$('#launchGame'), cover=$('#launchCover'), frame=$('#gameFrame'), frameShell=$('#browserFrameShell'); if(launch&&frame){launch.addEventListener('click',()=>{if(!frame.src)frame.src='../browser/index.html?embed=1'; cover?.classList.add('hidden');setTimeout(()=>frame.focus(),120)})}
$('#fullScreenGame')?.addEventListener('click',async()=>{if(!frame?.src)frame.src='../browser/index.html?embed=1';cover?.classList.add('hidden');try{if(frameShell?.requestFullscreen)await frameShell.requestFullscreen();else if(frameShell?.webkitRequestFullscreen)frameShell.webkitRequestFullscreen();setTimeout(()=>frame?.focus(),150)}catch(e){window.open('../browser/index.html','_blank')}});
$('#fullGame')?.addEventListener('click',()=>window.open('../browser/index.html','_blank'));
fetch(rootDepth+'site-config.json').then(r=>r.json()).then(c=>{$$('[data-version]').forEach(x=>x.textContent=c.version)}).catch(()=>{});
// Lightweight cinematic rift/ember field
const c=$('#riftCanvas'); if(c){const x=c.getContext('2d');let W,H,dpr,pts=[];function resize(){dpr=Math.min(devicePixelRatio||1,1.5);W=c.clientWidth;H=c.clientHeight;c.width=W*dpr;c.height=H*dpr;x.setTransform(dpr,0,0,dpr,0,0);pts=Array.from({length:95},()=>({x:Math.random()*W,y:Math.random()*H,z:Math.random(),v:.15+Math.random()*.65,a:Math.random()*6.28}))}resize();addEventListener('resize',resize);let t=0;function draw(){t+=.01;x.clearRect(0,0,W,H);const g=x.createRadialGradient(W*.76,H*.44,10,W*.76,H*.44,Math.min(W,H)*.5);g.addColorStop(0,'rgba(98,235,255,.16)');g.addColorStop(.23,'rgba(77,87,255,.08)');g.addColorStop(1,'rgba(0,0,0,0)');x.fillStyle=g;x.fillRect(0,0,W,H);for(const p of pts){p.y-=p.v*(.35+p.z);p.x+=Math.sin(t*2+p.a)*.2;if(p.y<-8){p.y=H+8;p.x=Math.random()*W}const a=.08+p.z*.5;x.fillStyle=`rgba(${p.z>.65?255:110},${p.z>.65?135:220},${p.z>.65?55:255},${a})`;x.fillRect(p.x,p.y,1+p.z*1.7,1+p.z*5)}requestAnimationFrame(draw)}draw()}
